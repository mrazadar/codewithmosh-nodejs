var _ = require('underscore');

// Core module
// File or folder
// node_modules

var res = _.contains([1, 2, 3], 2);
// console.log(`results`, res);

const lion = require('lion-lib-raza');
const results = lion.multiply(3, 5);

console.log(results);